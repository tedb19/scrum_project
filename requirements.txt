Django==1.8.3
djangorestframework==3.1.3
mysqlclient==1.3.6
wheel==0.24.0

# Markdown is used by the browsable API views
# to translate the docstrings into pages for
# help when users are viewing the API data.
Markdown==2.6.2

# enable filter translation from the query
# parameters to the ORM with django-filter
django-filter==0.10.0